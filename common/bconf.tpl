# no comment

export AUTOSIGNKEY="${autosign_key}"
export BUILDBRANCH="${build_branch}"
export BUILDDIR="${build_dir}"
export CALLBRANCH="${call_branch}"
export COMMUNITIES="${communities}"
export INFOFILE="${info_file}"
export LIBRARYDIR="${library_dir}"
export MKCMD="${mkcmd}"
export PRIORITY="${priority}"
export PYCMD="${pycmd}"
export RELEASE="${release}"
export STAGEDIR="${stage_dir}"
